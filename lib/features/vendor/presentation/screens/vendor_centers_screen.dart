import 'package:flutter/material.dart';

class VendorCentersScreen extends StatelessWidget {
  const VendorCentersScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('vendor CentersScreen'),
      ),
    );
  }
}
